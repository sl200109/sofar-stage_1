# 当前代码地图与交接总表

这份文档是当前阶段唯一的总入口，用来统一回答四类问题：

- 当前已经完成了哪些代码改动
- 每个主线代码文件分别负责什么功能
- 哪些代码对应我们的创新点
- 你下一步在服务器上该做什么、为什么做、上传哪些文件夹、回传哪些文件夹

文档分工固定如下：

- `当前代码地图与交接总表.md`：代码地图 + 当前操作总入口
- `baseline_stage5_tracker.md`：时间线、结论、风险判断
- `baseline_stage5_todolist.json`：任务状态机
- `交接操作.txt`：当前这一轮的服务器执行手册
- `so_far_pscr_实验设计报告.md`：论文叙事与工程落地附录，不承载交接细节

## 1. 当前阶段总状态

### 已完成
- `Stage 1-4`：baseline 固化、Part-aware parser、Dual-stage grounding、Stage 4 point cache 主链已经落地并形成可复用缓存。
- `Stage 5`：训练与回接结论已经收口。
  - `Open6DOR-only`：当前作为 `Open6DOR` 单域最优 head。
    - `best_val_loss = 0.376927`
    - `test mean_cosine = 0.289814`
    - `test mean_angle_deg = 67.6826`
  - `SpatialBench-only`：当前作为 `SpatialBench` 单域最优 head。
    - `best_val_loss = 0.223075`
    - `test mean_cosine = 0.554985`
    - `test mean_angle_deg = 49.1498`
  - `Combined / Balanced / Filtered / Curriculum`：当前阶段性判负，不再作为后续主线。
- `Stage 6/7/8`：规则版 agent、自动选择模式 agent、独立评测/消融框架已经开发完成，并已通过静态检查与服务器 smoke。
- `2026-04-22`：四个 `20-case smoke` 已全部完成。
- `2026-04-22`：`SpatialBench rule_v2` 的 `stage5_applicable14` targeted pilot 已完成。
- `2026-04-22`：`Stage 8 eval / ablation smoke` 已完成。

### 当前关键实验判断
- `SpatialBench`：
  - 顺序前 `20` 条 `Stage 8` 结果为：
    - `baseline total_accuracy = 0.25`
    - `direct_stage5 total_accuracy = 0.30`
    - `agent total_accuracy = 0.25`
  - 这组结果证明的是“前 20 条主要不是适合 Stage 5 的题型”，而不是“agent 无效”。
  - `stage5_applicable14` targeted pilot 才是当前更有解释力的结果：
    - `processed_samples = 14`
    - `total_accuracy = 0.4286`
    - `stage5 used = 11`
    - `verification accepted = 11`
  - 当前结论：`SpatialBench` 继续作为“适用子题 + agent routing / prompt / fallback”验证基准，不进入全数据正式训练。
- `Open6DOR`：
  - `Stage 8` 顺序前 `20` 条结果为：
    - `baseline valid_result_rate = 1.0`
    - `direct_stage5 valid_result_rate = 1.0`
    - `agent valid_result_rate = 1.0`
    - `agent stage5_acceptance_rate = 0.3`
    - `agent fallback_rate = 0.7`
    - `agent corrected_error_count = 13`
  - 这说明 `Open6DOR` agent controller 已经具备稳定的 direct / reject / fallback / shadow 行为。
  - 当前结论：`Open6DOR` 已满足进入下一轮正式训练的工程条件。

### 当前主判断
- `Open6DOR`：走“单目标操控朝向 + conditional verify / shadow debug”的 manipulation agent 主线，并直接开始下一轮正式训练。
- `SpatialBench`：走“题型筛选 + 模块路由 + evidence verify”的 VQA agent 主线，继续做 targeted 子题验证，不做全数据正式训练。
- 当前主线重点不是继续做更大规模 mixed training，而是：
  - 在 `Open6DOR` 上扩大有效训练数据并训练更稳的单域 head
  - 在 `SpatialBench` 上继续验证 agent 的适用性与跳过策略

## 2. 本轮代码改动总表

### 2.1 新加文件

| 文件 | 当前作用 | 所属阶段 | 新加/修改/复用 | 是否对应创新点 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| `sofar/serve/semantic_orientation_agent.py` | 统一规则版 agent controller，负责 SpatialBench / Open6DOR / auto route 的决策与 verify | Stage 6/7 | 新加 | 是 | done |
| `sofar/serve/agent_debug.py` | 统一写出 `output/agent_debug` 与 `output/agent_eval` 调试产物 | Stage 6/7/8 | 新加 | 是 | done |
| `sofar/analysis/stage8_agent_eval.py` | 独立运行 baseline / direct_stage5 / agent 三组评测 | Stage 8 | 新加 | 是 | done |
| `sofar/analysis/stage8_agent_ablation.py` | 顺序调用 Stage 8 eval，产出三组消融总表 | Stage 8 | 新加 | 是 | done |
| `sofar/spatialbench/pilots/stage5_applicable14.json` | SpatialBench 适用子题 pilot id 列表，用于定向补 cache 与 targeted smoke | Stage 6/7 | 新加 | 是 | done |

### 2.2 修改过的关键文件

| 文件 | 当前作用 | 所属阶段 | 新加/修改/复用 | 是否对应创新点 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| `sofar/spatialbench/eval_spatialbench.py` | SpatialBench 主入口；接入 Stage 5、dataset/auto agent、verify、trace 写盘、targeted pilot | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/open6dor/open6dor_perception.py` | Open6DOR 主入口；接入 direct / conditional / baseline / shadow 四类执行路径，并支持安全 JSON 导出 | Stage 5/6/7/8 | 修改 | 是 | done |
| `sofar/serve/stage5_inference.py` | 从 Stage 4 cache 运行 Stage 5 head，并把预测注入 scene graph | Stage 5 | 修改 | 是 | done |
| `sofar/serve/spatialbench_stage5.py` | SpatialBench `rule_v2` 题型分类、prompt variant 和方向语言化 | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/serve/qwen_inference.py` | 按 `prompt_variant` 切换 SpatialBench Stage 5 reasoning prompt | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/serve/system_prompts.py` | 新增 SpatialBench `axis/reference/camera` 三类 Stage 5 prompt | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/serve/batch_logging.py` | 输出 JSON 时支持更安全的序列化，减少 `Path` 类对象写盘报错 | 全阶段 | 修改 | 否 | done |

### 2.3 复用但未改的关键支撑文件

| 文件 | 当前作用 | 所属阶段 | 新加/修改/复用 | 是否对应创新点 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| `sofar/serve/stage3_grounding.py` | object-first / part-in-ROI grounding 缓存工具 | Stage 3 | 复用 | 是 | done |
| `sofar/serve/stage4_point_data.py` | point cache 构建与 geometry priors 计算 | Stage 4 | 复用 | 是 | done |
| `sofar/serve/runtime_paths.py` | checkpoint / dataset / output 路径解析 | 全阶段 | 复用 | 否 | done |
| `sofar/serve/stage5_manifest.py` | 从 Stage 4 cache 生成 Stage 5 训练/评测 manifest | Stage 5 | 复用 | 是 | done |
| `sofar/open6dor/utils.py` | Open6DOR 图像预处理、模板提示、最小对象集提取 | Stage 2/5 | 复用 | 否 | done |
| `sofar/orientation/datasets/Stage4PointCache.py` | Stage 4 cache 到训练 batch 的 Dataset/DataLoader 适配层 | Stage 5 | 复用 | 是 | done |
| `sofar/orientation/models/PartConditionedOrientationHead.py` | Part-conditioned Orientation Head 本体 | Stage 5 | 复用 | 是 | done |
| `sofar/orientation/stage5_dry_run.py` | Stage 5 最小 dry-run 入口 | Stage 5 | 复用 | 否 | done |
| `sofar/orientation/stage5_train_pilot.py` | Stage 5 首轮与后续正式训练入口 | Stage 5 | 复用 | 否 | done |
| `sofar/orientation/stage5_eval_unseen.py` | Stage 5 未见样本评测入口 | Stage 5 | 复用 | 否 | done |

## 3. 主线代码地图

说明：这一节覆盖“当前主线入口 + 关键支撑文件”，用于交接与定位，不追求枚举仓库中所有辅助文件。

### 3.1 `serve`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/serve/stage5_inference.py` | `Stage 4 cache dir`、checkpoint 路径、device、点数；也接收 scene graph | `stage5 prediction dict`、`target_orientation`、注入后的 scene graph | `open6dor_perception.py`、`eval_spatialbench.py` | 把 Stage 5 变成可调用工具层，而不是只存在于训练脚本里 |
| `sofar/serve/spatialbench_stage5.py` | `question`、`options`、`parser_info`、`stage5 prediction` | `applicability gate`、`category`、`prompt_variant`、`readable_summary`、`axis_hint` | `eval_spatialbench.py`、`stage5_inference.py` | 把“数值方向向量”翻译成更适合 VQA 使用的题型判断、prompt 分流和语言线索 |
| `sofar/serve/semantic_orientation_agent.py` | SpatialBench / Open6DOR 的 parser、task type、mode、cache、confidence、Stage 5 signals | 统一 `AgentDecision`、`verification_status`、`selected_execution_mode` | `eval_spatialbench.py`、`open6dor_perception.py` | Stage 6/7 核心控制器，是规则版 Re-Verification + Module-Selection + AutoModeSelection 的实现中心 |
| `sofar/serve/agent_debug.py` | 每样本 agent record、run_id、dataset、mode | `output/agent_debug/...`、`output/agent_eval/...` 下的 json/jsonl/csv | `eval_spatialbench.py`、`open6dor_perception.py`、`stage8_agent_eval.py` | 把 agent 行为从散落日志变成集中可分析调试空间 |
| `sofar/serve/qwen_inference.py` | Qwen model、processor、image、instruction、scene graph、`stage5_context` | parser JSON、Open6DOR reasoning JSON、VQA answer | `eval_spatialbench.py`、`open6dor_perception.py` | 当前 parser 与 reasoning 的统一后端，也是 SpatialBench `rule_v2` 按子题型切 prompt 的执行入口 |
| `sofar/serve/system_prompts.py` | 无显式运行时输入，作为 prompt 常量库 | baseline prompt、通用 Stage 5 prompt、`axis/reference/camera` 三类 SpatialBench Stage 5 prompt | `qwen_inference.py` | 负责 baseline reasoning 与子题型 orientation-aware reasoning 的 prompt 分流 |
| `sofar/serve/batch_logging.py` | 输出目录、前缀、要写出的 json 数据 | 带时间戳的 log、run_id、稳定命名 json | `eval_spatialbench.py`、`open6dor_perception.py`、`stage8_agent_eval.py` | 保证每一轮 smoke/eval 都有可回溯日志与稳定 summary |
| `sofar/serve/stage3_grounding.py` | detections、ROI、mask、image size | object/part bbox、mask、ROI meta、Stage 3 cache 文件 | `eval_spatialbench.py`、`open6dor_perception.py` | 把“先 object 再 part”的 grounding 逻辑固化为可缓存的 Stage 3 |
| `sofar/serve/stage4_point_data.py` | `xyz_points`、RGB image、object/part mask | object/part 点云、geometry priors、Stage 4 cache 文件 | `eval_spatialbench.py`、`open6dor_perception.py` | 把视觉证据变成 Stage 5 可训练 / 可推理的数据格式 |
| `sofar/serve/runtime_paths.py` | 环境变量或默认仓库根目录 | checkpoints、datasets、output 等标准路径 | 多个主线脚本 | 保证本地与服务器路径切换时不需要到处手改路径 |
| `sofar/serve/stage5_manifest.py` | `repo_root`、`output_dir`、Stage 4 cache / records | Stage 5 smoke/train/eval manifests 与 summary | `stage5_dry_run.py`、`stage5_train_pilot.py`、`stage5_eval_unseen.py` | 是 Stage 4 cache 向 Stage 5 Dataset/DataLoader 过渡的桥 |

### 3.2 `open6dor`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/open6dor/open6dor_perception.py` | Open6DOR task dirs、CLI flags、Stage 5 checkpoint、agent mode | 每任务 `result.json`、progress/summary、`stage5_open6dor_pipeline_records.json`、`agent_debug/open6dor/...` | 命令行直接运行；被 `stage8_agent_eval.py` 间接调用 | Open6DOR 全链主入口，也是 manipulation agent 的落地点 |
| `sofar/open6dor/utils.py` | instruction、object name、orientation templates、image | 预处理图像、模板提示、最小对象集、标准化 object 名称 | `open6dor_perception.py`、`stage5_inference.py` | 给 Open6DOR parser、Stage 5 attr 归一化、模板提示提供支撑 |

### 3.3 `spatialbench`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/spatialbench/eval_spatialbench.py` | SpatialBench 样本、CLI flags、Stage 4 cache、Stage 5 checkpoint、agent mode | `eval_spatialbench_progress.json`、csv、log、`agent_debug/spatialbench/...` | 命令行直接运行；被 `stage8_agent_eval.py` 间接调用 | SpatialBench 全链主入口，也是 VQA task-aware agent 的落地点 |
| `sofar/spatialbench/pilots/stage5_applicable14.json` | 无运行时输入；作为预定义 sample id 列表 | `14` 条 targeted sample id | `eval_spatialbench.py` 通过 `--pilot stage5_applicable14` 使用 | 让 SpatialBench 从“顺序前 20 条几乎全不适配”切换为“定向测试真正适合 Stage 5 的题型” |

### 3.4 `orientation`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/orientation/stage5_dry_run.py` | repo_root、manifest 目录、prefer_dataset、batch/points/device | dry-run summary、loss、选中的 manifest | 命令行直接运行 | 验证 Stage 4 cache、Dataset、Head 前向/反向是否能最小闭环跑通 |
| `sofar/orientation/stage5_train_pilot.py` | repo_root、dataset_mode、manifest、训练超参、初始 checkpoint | train/val/test manifest、best/last checkpoint、训练 summary | 命令行直接运行 | 既是首轮训练入口，也是下一轮 Open6DOR 正式训练入口 |
| `sofar/orientation/stage5_eval_unseen.py` | checkpoint、dataset_mode、subset、manifest、batch/points/device | 未见样本 metrics summary 与 eval manifest | 命令行直接运行 | 用于评估 Stage 5 在未训练样本上的泛化表现 |
| `sofar/orientation/datasets/Stage4PointCache.py` | Stage 5 manifest 中的 point cache 路径、points 数量 | 训练/评测 batch：`points`、`target_direction`、`label_confidence`、`prior_vector`、`instruction` | `stage5_dry_run.py`、`stage5_train_pilot.py`、`stage5_eval_unseen.py` | 统一 Stage 4 cache 到神经网络输入的读取与采样逻辑 |
| `sofar/orientation/models/PartConditionedOrientationHead.py` | 组合点云、文本 instruction、geometry prior vector | `3D direction vector` | `stage5_inference.py`、三条 `orientation` 脚本 | 当前 Stage 5 的核心 head，本质是重新训练的预测头，不是整体微调大模型 |

### 3.5 `analysis`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/analysis/stage8_agent_eval.py` | dataset、eval-mode、agent-mode、checkpoint、limit、output-dir、`--reuse-source` | 每组 `summary.json`、`per_sample.jsonl`、`metrics_by_bucket.json`、`decision_breakdown.csv` | 命令行直接运行；被 `stage8_agent_ablation.py` 调用 | 把 baseline / direct_stage5 / agent 的比较独立成评测框架，不依赖训练脚本 |
| `sofar/analysis/stage8_agent_ablation.py` | dataset、agent-mode、checkpoint、limit、output-dir、`--reuse-source` | `ablation_summary.json`、`ablation_table.csv` | 命令行直接运行 | 顺序跑三组模式，快速形成论文和分析用的 ablation 表 |

## 4. 创新点与代码对应表

| 创新点 | 创新点本身是什么 | 当前已经落地到哪些代码 | 当前档位 | 备注 |
| --- | --- | --- | --- | --- |
| `PSCR backbone` | 从 parser、grounding、point cache 到 part-conditioned head 的主干链路 | `qwen_inference.py`、`stage3_grounding.py`、`stage4_point_data.py`、`stage5_manifest.py`、`Stage4PointCache.py`、`PartConditionedOrientationHead.py`、`stage5_inference.py` | 代码完成 | Stage 1-5 结论已收口，单域 head 已训练与回接 |
| `SpatialBench task-aware gating` | 先判断题型是否适合 Stage 5，再决定是否注入 orientation evidence；当前已升级为 `rule_v2` 子题型拆分 | `spatialbench_stage5.py`、`eval_spatialbench.py`、`system_prompts.py`、`qwen_inference.py` | 代码完成 + 服务器验证完成 | `stage5_applicable14` targeted pilot 已经说明这条线在适用子题上是可用的 |
| `Re-Verification Agent` | 对 Stage 5 结果做证据完整性或模式一致性验证，失败则回退 | `semantic_orientation_agent.py`、`eval_spatialbench.py`、`open6dor_perception.py` | 代码完成 + 服务器验证完成 | `Open6DOR` 已经真实触发 reject / fallback / shadow，`SpatialBench` 已触发 accepted / not_run |
| `Module-Selection Agent` | 在已有模块之间做 task-aware routing：启用、跳过、fallback、prompt switch | `semantic_orientation_agent.py`、`eval_spatialbench.py`、`open6dor_perception.py` | 代码完成 + 服务器验证完成 | `SpatialBench` 更偏题型路由，`Open6DOR` 更偏执行带选择 |
| `AutoModeSelection Agent` | 自动识别当前样本属于哪个数据集，并转发给对应数据集 agent | `semantic_orientation_agent.py`、`eval_spatialbench.py`、`open6dor_perception.py` | 代码完成 + 服务器验证完成 | `SpatialBench` 与 `Open6DOR` 的 `20-case auto smoke` 均已完成 |
| `Stage 8 独立评测/消融框架` | 用独立 runner 对 baseline / direct_stage5 / agent 三组结果做统一汇总 | `stage8_agent_eval.py`、`stage8_agent_ablation.py`、`agent_debug.py` | 代码完成 + 服务器验证完成 | 结果已经落地，但 `Open6DOR` 指标仍需后续替换为更严格的 GT orientation 指标 |

## 5. 当前你要做的操作

### Step 0：哪些已经完成，不要再重复跑
- 已完成，不要重复跑：
  - 四个 `20-case smoke`
  - `SpatialBench stage5_applicable14` targeted pilot
  - `Stage 8` 的八条 `eval / ablation` 命令
- 现在的重点已经不是再跑 smoke，而是进入 `Open6DOR` 下一轮正式训练。

### Step 1：先上传代码
- 先上传的文件夹：
  - `sofar/open6dor`
  - `sofar/serve`
  - `sofar/orientation`
- 只有在服务器还缺最新 `Stage 8 reuse-source` 工具时，再补传：
  - `sofar/analysis`
- 只有在服务器没有上一轮 `Open6DOR-only best checkpoint` 时，再补传：
  - `sofar/output/stage5_open6dor_train`

### Step 2：增量补 Open6DOR Stage 3/4 cache
- 命令：

```bash
cd /data/coding/SoFar
python open6dor/open6dor_perception.py --stage3-grounding-only --limit 100 --speed-profile off
python open6dor/open6dor_perception.py --stage4-pointdata-only --limit 100 --speed-profile off
```

- 意义：
  - 这两条命令都是增量模式。
  - 脚本会先跳过已有 Stage 3/4 cache，再对 pending case 应用 `--limit 100`。
  - 因此这一步不会把已经做过的 Open6DOR case 整批重跑。
- 做完后要检查什么：
  - 日志里出现 `skipped X tasks with existing Stage 3 cache` / `Stage 4 cache`
  - 日志里出现 `... reduced to 100 pending tasks` 或更小的 pending 数
- 做完后要回传哪些文件夹：
  - 这一步先不用回传

### Step 3：做一次 Open6DOR dry-run
- 命令：

```bash
cd /data/coding/SoFar
python orientation/stage5_dry_run.py --repo-root /data/coding/SoFar --prefer-dataset open6dor --batch-size 4 --num-points 1024 --max-samples 16 --device cuda
```

- 意义：先确认扩容后的 manifest、Dataset、DataLoader、Head 前向还是通的。
- 做完后要检查什么：
  - 输出里出现 `manifest_path / dataset_size / loss / summary_path`
- 做完后要回传哪些文件夹：
  - 这一步先不用回传

### Step 4：开始 Open6DOR-only 正式训练 Round 1
- 命令：

```bash
cd /data/coding/SoFar
python orientation/stage5_train_pilot.py --repo-root /data/coding/SoFar --output-dir /data/coding/SoFar/output/stage5_open6dor_train_round1_formal --dataset-mode open6dor --batch-size 4 --num-points 1024 --max-train-samples 320 --max-val-samples 64 --max-test-samples 64 --epochs 30 --min-label-confidence 0.3 --init-checkpoint /data/coding/SoFar/output/stage5_open6dor_train/stage5_pilot_best.pth --device cuda
```

- 意义：
  - 这是下一轮正式训练的主命令。
  - 当前明确只训练 `Open6DOR-only`，不做 mixed training。
  - 使用上一轮 `Open6DOR-only best checkpoint` warm-start，减少从头训练的波动。
- 做完后要检查什么：
  - 输出目录：`sofar/output/stage5_open6dor_train_round1_formal`
  - 至少生成：
    - `stage5_train_manifest.jsonl`
    - `stage5_val_manifest.jsonl`
    - `stage5_test_manifest.jsonl`
    - `stage5_pilot_best.pth`
    - `stage5_pilot_last.pth`
  - summary 中能看到：
    - `train_size / val_size / test_size`
    - `best_val_loss`
    - `final_test_metrics`
- 如果服务器显存紧张：
  - 先把 `--batch-size 4` 改成 `2` 再重试，不要先改别的逻辑。

### Step 5：做未见样本评测
- 命令：

```bash
cd /data/coding/SoFar
python orientation/stage5_eval_unseen.py --repo-root /data/coding/SoFar --output-dir /data/coding/SoFar/output/stage5_open6dor_train_round1_formal --checkpoint /data/coding/SoFar/output/stage5_open6dor_train_round1_formal/stage5_pilot_best.pth --dataset-mode open6dor --subset test --batch-size 4 --num-points 1024 --max-samples 128 --device cuda
```

- 意义：统一评估新 checkpoint 在未见样本上的泛化。
- 做完后要检查什么：
  - 输出里出现 `weighted_loss / mean_cosine / mean_angle_deg / median_angle_deg`
- 做完后要回传哪些文件夹：
  - 先不用回传，和下一步一起回传更省事

### Step 6：用新 checkpoint 做一次 Open6DOR 20-case 回接 smoke
- 命令：

```bash
cd /data/coding/SoFar
python open6dor/open6dor_perception.py --limit 20 --reset-progress --rerun-existing --use-stage5-head --stage5-checkpoint /data/coding/SoFar/output/stage5_open6dor_train_round1_formal/stage5_pilot_best.pth --agent-mode dataset --agent-save-trace --agent-shadow-eval --speed-profile conservative
```

- 意义：验证“新训练出的 checkpoint 能不能稳定回接完整 Open6DOR pipeline”。
- 做完后要检查什么：
  - `20/20` 正常跑完
  - `open6dor_perception_summary_*.json` 正常生成
  - `stage5_open6dor_pipeline_records_*.json` 正常生成
  - `agent_debug/open6dor/<run_id>/` 正常生成
- 做完后要回传哪些文件夹：
  - 见下方统一回传规则

## 6. 上传 / 回传文件夹规则

### 6.1 最小代码同步
- 上传优先写文件夹：
  - `sofar/open6dor`
  - `sofar/serve`
  - `sofar/orientation`
- 只有在服务器缺少最新评测工具时，再补传：
  - `sofar/analysis`
- 只有在服务器缺少上一轮 warm-start checkpoint 时，再补传：
  - `sofar/output/stage5_open6dor_train`

### 6.2 Open6DOR 正式训练回传
- 回传优先写文件夹：
  - `sofar/output/stage5_open6dor_train_round1_formal`
  - `sofar/output/agent_debug`
- 必要补充文件：
  - 本轮最新 `sofar/output/open6dor_perception_summary_*.json`
  - 本轮最新 `sofar/output/stage5_open6dor_pipeline_records_*.json`
  - `sofar/output/stage5_manifest_summary.json`
  - 本轮最新 `sofar/output/open6dor_perception_*.log`

### 6.3 Stage 8 结果读取规则
- 由于同一个 `sofar/output/agent_eval_smoke` 根目录被多次复用，根目录下的 `summary.json` 与 `ablation_summary.json` 只代表最后一次运行的数据集。
- 后续分析必须以子目录为准：
  - `sofar/output/agent_eval_smoke/spatialbench/...`
  - `sofar/output/agent_eval_smoke/open6dor/...`

## 7. 当前缺口与下一步

### 当前缺口
- `Open6DOR`：
  - 工程上已经可以开始正式训练，但 `Stage 8` 里的 `correct` 仍然是 `status == success`，不是最终 GT orientation 指标。
  - 因此下一步可以推进训练，但论文结论里还不能把当前 `Stage 8` 结果直接写成“orientation 性能提升”。
- `SpatialBench`：
  - targeted 子题已经证明 rule_v2 可用，但还不足以支持“全数据正式训练”这条线。

### 下一步
- 立即开始：`Open6DOR` 正式训练 Round 1。
- 暂缓推进：
  - `SpatialBench` 全数据正式训练
  - 新的 mixed training 变体
- 后续需要补的一项评测工作：
  - 把 `Open6DOR Stage 8` 的指标从“pipeline success”替换为更真实的 orientation correctness 指标。

