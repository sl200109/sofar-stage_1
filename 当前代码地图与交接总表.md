# 当前代码地图与交接总表

这份文档是当前阶段唯一的总入口，用来统一回答四类问题：

- 当前已经完成了哪些代码改动
- 每个主线代码文件分别负责什么功能
- 哪些代码对应我们的创新点
- 你下一步在服务器上该做什么、为什么做、上传哪些文件夹、回传哪些文件夹

文档分工固定如下：

- `当前代码地图与交接总表.md`：代码地图 + 当前操作总入口
- `baseline_stage5_tracker.md`：时间线、结论、风险判断
- `baseline_stage5_todolist.json`：任务状态机
- `交接操作.txt`：当前这一轮的服务器执行手册
- `so_far_pscr_实验设计报告.md`：论文叙事与工程落地附录，不承载交接细节

## 1. 当前阶段总状态

### 已完成
- `Stage 1-4`：baseline 固化、Part-aware parser、Dual-stage grounding、Stage 4 point cache 主链已经落地并形成可复用缓存。
- `Stage 5`：训练与回接结论已经收口。
  - `Open6DOR-only`：当前作为 `Open6DOR` 单域最优 head。
    - `best_val_loss = 0.376927`
    - `test mean_cosine = 0.289814`
    - `test mean_angle_deg = 67.6826`
  - `SpatialBench-only`：当前作为 `SpatialBench` 单域最优 head。
    - `best_val_loss = 0.223075`
    - `test mean_cosine = 0.554985`
    - `test mean_angle_deg = 49.1498`
  - `Combined / Balanced / Filtered / Curriculum`：当前阶段性判负，不再作为后续主线。
- `Stage 6/7/8`：本地代码已经完成规则版 agent 与独立评测框架开发，并通过静态检查。
  - `python -m py_compile sofar/serve/semantic_orientation_agent.py`
  - `python -m py_compile sofar/serve/agent_debug.py`
  - `python -m py_compile sofar/spatialbench/eval_spatialbench.py`
  - `python -m py_compile sofar/open6dor/open6dor_perception.py`
  - `python -m py_compile sofar/analysis/stage8_agent_eval.py`
  - `python -m py_compile sofar/analysis/stage8_agent_ablation.py`
- `2026-04-22`：`SpatialBench rule_v2` 已在本地落地。
  - 新增更细的题型拆分：`axis_direction / reference_alignment / camera_alignment`
  - 放宽“单目标 + 单参照物”场景，不再把 `parser_object_count = 2` 一律视为歧义
  - Stage 5 prompt 改成按子题型切换，而不是统一使用一个 orientation-aware prompt

### 待服务器验证
- `SpatialBench` 20-case `dataset agent smoke`
- `Open6DOR` 20-case `dataset agent smoke`
- `SpatialBench` 20-case `auto agent smoke`
- `Open6DOR` 20-case `auto agent smoke`
- `Stage 8` baseline / direct_stage5 / agent 三组 `eval smoke`
- `Stage 8` `ablation smoke`

### 当前主判断
- `Open6DOR`：走“单目标操控朝向 + conditional verify / shadow debug”的 manipulation agent 主线。
- `SpatialBench`：走“题型筛选 + 模块路由 + evidence verify”的 VQA agent 主线。
- 当前主线重点不是继续做更大规模 mixed training，而是验证 agent 是否真的减少错误注入、是否能正确跳过不适配题型。

## 2. 本轮代码改动总表

### 2.1 新加文件

| 文件 | 当前作用 | 所属阶段 | 新加/修改/复用 | 是否对应创新点 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| `sofar/serve/semantic_orientation_agent.py` | 统一规则版 agent controller，负责 SpatialBench / Open6DOR / auto route 的决策与 verify | Stage 6/7 | 新加 | 是 | done |
| `sofar/serve/agent_debug.py` | 统一写出 `output/agent_debug` 与 `output/agent_eval` 调试产物 | Stage 6/7/8 | 新加 | 是 | done |
| `sofar/analysis/stage8_agent_eval.py` | 独立运行 baseline / direct_stage5 / agent 三组评测 | Stage 8 | 新加 | 是 | done |
| `sofar/analysis/stage8_agent_ablation.py` | 顺序调用 Stage 8 eval，产出三组消融总表 | Stage 8 | 新加 | 是 | done |
| `sofar/spatialbench/pilots/stage5_applicable14.json` | SpatialBench 适用子题 pilot id 列表，用于定向补 cache 与 targeted smoke | Stage 6/7 | 新加 | 是 | done |

### 2.2 修改过的关键文件

| 文件 | 当前作用 | 所属阶段 | 新加/修改/复用 | 是否对应创新点 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| `sofar/spatialbench/eval_spatialbench.py` | SpatialBench 主入口；接入 Stage 5、dataset/auto agent、verify、trace 写盘 | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/open6dor/open6dor_perception.py` | Open6DOR 主入口；接入 direct / conditional / baseline / shadow 四类执行路径 | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/serve/stage5_inference.py` | 从 Stage 4 cache 运行 Stage 5 head，并把预测注入 scene graph | Stage 5 | 修改 | 是 | done |
| `sofar/serve/spatialbench_stage5.py` | SpatialBench `rule_v2` 题型分类、prompt variant 和方向语言化 | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/serve/qwen_inference.py` | 按 `prompt_variant` 切换 SpatialBench Stage 5 reasoning prompt | Stage 5/6/7 | 修改 | 是 | done |
| `sofar/serve/system_prompts.py` | 新增 SpatialBench `axis/reference/camera` 三类 Stage 5 prompt | Stage 5/6/7 | 修改 | 是 | done |

### 2.3 复用但未改的关键支撑文件

| 文件 | 当前作用 | 所属阶段 | 新加/修改/复用 | 是否对应创新点 | 当前状态 |
| --- | --- | --- | --- | --- | --- |
| `sofar/serve/batch_logging.py` | run_id、日志镜像、json 输出稳定写盘 | 全阶段 | 复用 | 否 | done |
| `sofar/serve/stage3_grounding.py` | object-first / part-in-ROI grounding 缓存工具 | Stage 3 | 复用 | 是 | done |
| `sofar/serve/stage4_point_data.py` | point cache 构建与 geometry priors 计算 | Stage 4 | 复用 | 是 | done |
| `sofar/serve/runtime_paths.py` | checkpoint / dataset / output 路径解析 | 全阶段 | 复用 | 否 | done |
| `sofar/serve/stage5_manifest.py` | 从 Stage 4 cache 生成 Stage 5 训练/评测 manifest | Stage 5 | 复用 | 是 | done |
| `sofar/open6dor/utils.py` | Open6DOR 图像预处理、模板提示、最小对象集提取 | Stage 2/5 | 复用 | 否 | done |
| `sofar/orientation/datasets/Stage4PointCache.py` | Stage 4 cache 到训练 batch 的 Dataset/DataLoader 适配层 | Stage 5 | 复用 | 是 | done |
| `sofar/orientation/models/PartConditionedOrientationHead.py` | Part-conditioned Orientation Head 本体 | Stage 5 | 复用 | 是 | done |
| `sofar/orientation/stage5_dry_run.py` | Stage 5 最小 dry-run 入口 | Stage 5 | 复用 | 否 | done |
| `sofar/orientation/stage5_train_pilot.py` | Stage 5 首轮训练入口 | Stage 5 | 复用 | 否 | done |
| `sofar/orientation/stage5_eval_unseen.py` | Stage 5 未见样本评测入口 | Stage 5 | 复用 | 否 | done |

## 3. 主线代码地图

说明：这一节覆盖“当前主线入口 + 关键支撑文件”，用于交接与定位，不追求枚举仓库中所有辅助文件。

### 3.1 `serve`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/serve/stage5_inference.py` | `Stage 4 cache dir`、checkpoint 路径、device、点数；也接收 scene graph | `stage5 prediction dict`、`target_orientation`、注入后的 scene graph | `open6dor_perception.py`、`eval_spatialbench.py` | 把 Stage 5 变成可调用工具层，而不是只存在于训练脚本里 |
| `sofar/serve/spatialbench_stage5.py` | `question`、`options`、`parser_info`、`stage5 prediction` | `applicability gate`、`category`、`prompt_variant`、`readable_summary`、`axis_hint` | `eval_spatialbench.py`、`stage5_inference.py` | 把“数值方向向量”翻译成更适合 VQA 使用的题型判断、prompt 分流和语言线索 |
| `sofar/serve/semantic_orientation_agent.py` | SpatialBench / Open6DOR 的 parser、task type、mode、cache、confidence、Stage 5 signals | 统一 `AgentDecision`、`verification_status`、`selected_execution_mode` | `eval_spatialbench.py`、`open6dor_perception.py` | Stage 6/7 核心控制器，是规则版 Re-Verification + Module-Selection + AutoModeSelection 的实现中心 |
| `sofar/serve/agent_debug.py` | 每样本 agent record、run_id、dataset、mode | `output/agent_debug/...`、`output/agent_eval/...` 下的 json/jsonl/csv | `eval_spatialbench.py`、`open6dor_perception.py`、`stage8_agent_eval.py` | 把 agent 行为从散落日志变成集中可分析调试空间 |
| `sofar/serve/qwen_inference.py` | Qwen model、processor、image、instruction、scene graph、`stage5_context` | parser JSON、Open6DOR reasoning JSON、VQA answer | `eval_spatialbench.py`、`open6dor_perception.py` | 当前 parser 与 reasoning 的统一后端，也是 SpatialBench `rule_v2` 按子题型切 prompt 的执行入口 |
| `sofar/serve/system_prompts.py` | 无显式运行时输入，作为 prompt 常量库 | baseline prompt、通用 Stage 5 prompt、`axis/reference/camera` 三类 SpatialBench Stage 5 prompt | `qwen_inference.py` | 负责 baseline reasoning 与子题型 orientation-aware reasoning 的 prompt 分流 |
| `sofar/serve/batch_logging.py` | 输出目录、前缀、要写出的 json 数据 | 带时间戳的 log、run_id、稳定命名 json | `eval_spatialbench.py`、`open6dor_perception.py`、`stage8_agent_eval.py` | 保证每一轮 smoke/eval 都有可回溯日志与稳定 summary |
| `sofar/serve/stage3_grounding.py` | detections、ROI、mask、image size | object/part bbox、mask、ROI meta、Stage 3 cache 文件 | `eval_spatialbench.py`、`open6dor_perception.py` | 把“先 object 再 part”的 grounding 逻辑固化为可缓存的 Stage 3 |
| `sofar/serve/stage4_point_data.py` | `xyz_points`、RGB image、object/part mask | object/part 点云、geometry priors、Stage 4 cache 文件 | `eval_spatialbench.py`、`open6dor_perception.py` | 把视觉证据变成 Stage 5 可训练 / 可推理的数据格式 |
| `sofar/serve/runtime_paths.py` | 环境变量或默认仓库根目录 | checkpoints、datasets、output 等标准路径 | 多个主线脚本 | 保证本地与服务器路径切换时不需要到处手改路径 |
| `sofar/serve/stage5_manifest.py` | `repo_root`、`output_dir`、Stage 4 cache / records | Stage 5 smoke/train/eval manifests 与 summary | `stage5_dry_run.py`、`stage5_train_pilot.py`、`stage5_eval_unseen.py` | 是 Stage 4 cache 向 Stage 5 Dataset/DataLoader 过渡的桥 |

### 3.2 `open6dor`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/open6dor/open6dor_perception.py` | Open6DOR task dirs、CLI flags、Stage 5 checkpoint、agent mode | 每任务 `result.json`、progress/summary、`stage5_open6dor_pipeline_records.json`、`agent_debug/open6dor/...` | 命令行直接运行；被 `stage8_agent_eval.py` 间接调用 | Open6DOR 全链主入口，也是 manipulation agent 的落地点 |
| `sofar/open6dor/utils.py` | instruction、object name、orientation templates、image | 预处理图像、模板提示、最小对象集、标准化 object 名称 | `open6dor_perception.py`、`stage5_inference.py` | 给 Open6DOR parser、Stage 5 attr 归一化、模板提示提供支撑 |

### 3.3 `spatialbench`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/spatialbench/eval_spatialbench.py` | SpatialBench 样本、CLI flags、Stage 4 cache、Stage 5 checkpoint、agent mode | `eval_spatialbench_progress.json`、csv、log、`agent_debug/spatialbench/...` | 命令行直接运行；被 `stage8_agent_eval.py` 间接调用 | SpatialBench 全链主入口，也是 VQA task-aware agent 的落地点 |
| `sofar/spatialbench/pilots/stage5_applicable14.json` | 无运行时输入；作为预定义 sample id 列表 | `14` 条 `single_object_direction` 样本 id | `eval_spatialbench.py` 通过 `--pilot stage5_applicable14` 使用 | 让 SpatialBench 从“顺序前 20 条全 skip”切换为“定向测试真正适配的 Stage 5 子题” |

### 3.4 `orientation`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/orientation/stage5_dry_run.py` | repo_root、manifest 目录、prefer_dataset、batch/points/device | dry-run summary、loss、选中的 manifest | 命令行直接运行 | 验证 Stage 4 cache、Dataset、Head 前向/反向是否能最小闭环跑通 |
| `sofar/orientation/stage5_train_pilot.py` | repo_root、dataset_mode、manifest、训练超参、初始 checkpoint | train/val/test manifest、best/last checkpoint、训练 summary | 命令行直接运行 | Stage 5 首轮训练入口，用来验证单域 head 是否可学 |
| `sofar/orientation/stage5_eval_unseen.py` | checkpoint、dataset_mode、subset、manifest、batch/points/device | 未见样本 metrics summary 与 eval manifest | 命令行直接运行 | 用于评估 Stage 5 在未训练样本上的泛化表现 |
| `sofar/orientation/datasets/Stage4PointCache.py` | Stage 5 manifest 中的 point cache 路径、points 数量 | 训练/评测 batch：`points`、`target_direction`、`label_confidence`、`prior_vector`、`instruction` | `stage5_dry_run.py`、`stage5_train_pilot.py`、`stage5_eval_unseen.py` | 统一 Stage 4 cache 到神经网络输入的读取与采样逻辑 |
| `sofar/orientation/models/PartConditionedOrientationHead.py` | 组合点云、文本 instruction、geometry prior vector | `3D direction vector` | `stage5_inference.py`、三条 `orientation` 脚本 | 当前 Stage 5 的核心 head，本质是重新训练的预测头，不是整体微调大模型 |

### 3.5 `analysis`

| 文件 | 输入是什么 | 输出是什么 | 被谁调用 | 对当前主线的意义 |
| --- | --- | --- | --- | --- |
| `sofar/analysis/stage8_agent_eval.py` | dataset、eval-mode、agent-mode、checkpoint、limit、output-dir | 每组 `summary.json`、`per_sample.jsonl`、`metrics_by_bucket.json`、`decision_breakdown.csv` | 命令行直接运行；被 `stage8_agent_ablation.py` 调用 | 把 baseline / direct_stage5 / agent 的比较独立成评测框架，不依赖训练脚本 |
| `sofar/analysis/stage8_agent_ablation.py` | dataset、agent-mode、checkpoint、limit、output-dir | `ablation_summary.json`、`ablation_table.csv` | 命令行直接运行 | 顺序跑三组模式，快速形成论文和分析用的 ablation 表 |

## 4. 创新点与代码对应表

| 创新点 | 创新点本身是什么 | 当前已经落地到哪些代码 | 当前档位 | 备注 |
| --- | --- | --- | --- | --- |
| `PSCR backbone` | 从 parser、grounding、point cache 到 part-conditioned head 的主干链路 | `qwen_inference.py`、`stage3_grounding.py`、`stage4_point_data.py`、`stage5_manifest.py`、`Stage4PointCache.py`、`PartConditionedOrientationHead.py`、`stage5_inference.py` | 代码完成 | Stage 1-5 结论已收口，单域 head 已训练与回接 |
| `SpatialBench task-aware gating` | 先判断题型是否适合 Stage 5，再决定是否注入 orientation evidence；当前已升级为 `rule_v2` 子题型拆分 | `spatialbench_stage5.py`、`eval_spatialbench.py`、`system_prompts.py`、`qwen_inference.py` | 代码完成 | 已从单一 `single_object_direction` 扩成 `axis/reference/camera` 三类适用子题，待服务器 smoke 验证效果 |
| `Re-Verification Agent` | 对 Stage 5 结果做证据完整性或模式一致性验证，失败则回退 | `semantic_orientation_agent.py`、`eval_spatialbench.py`、`open6dor_perception.py` | 服务器验证未完成 | 规则版代码已经接上，但还需要 dataset/auto smoke 证实行为稳定 |
| `Module-Selection Agent` | 在已有模块之间做 task-aware routing：启用、跳过、fallback、prompt switch | `semantic_orientation_agent.py`、`eval_spatialbench.py`、`open6dor_perception.py` | 服务器验证未完成 | SpatialBench 更偏题型路由，Open6DOR 更偏执行带选择 |
| `AutoModeSelection Agent` | 自动识别当前样本属于哪个数据集，并转发给对应数据集 agent | `semantic_orientation_agent.py`、`eval_spatialbench.py`、`open6dor_perception.py` | 服务器验证未完成 | 本地 `auto` 路由代码已完成，待服务器 smoke 看分布是否正确 |
| `Stage 8 独立评测/消融框架` | 用独立 runner 对 baseline / direct_stage5 / agent 三组结果做统一汇总 | `stage8_agent_eval.py`、`stage8_agent_ablation.py`、`agent_debug.py` | 服务器验证未完成 | 代码完成，结果表还没在服务器真正跑出来 |

## 5. 当前你要做的操作

### Step 0：先上传代码
- 命令：无命令，先同步代码文件夹。
- 意义：先让服务器端和本地当前 agent 代码一致，再做 smoke / eval / ablation。
- 做完后要检查什么：服务器上的 `/data/coding/SoFar` 中，`serve / spatialbench / open6dor / analysis` 已经是这一版。
- 做完后要回传哪些文件夹：这一步本身不回传结果。
- 先上传的文件夹：
  - `sofar/serve`
  - `sofar/spatialbench`
  - `sofar/open6dor`
  - `sofar/analysis`
- 只有在服务器训练代码或 checkpoint 不一致时，再补传：
  - `sofar/orientation`
  - `sofar/output/stage5_open6dor_train`
  - `sofar/output/stage5_spatialbench_train`

### Step 1：SpatialBench 20-case dataset agent smoke
- 命令：

```bash
cd /data/coding/SoFar
python spatialbench/eval_spatialbench.py --limit 20 --reset-progress --use-stage5-head --agent-mode dataset --agent-save-trace --speed-profile conservative
```

- 意义：验证 `SpatialBench` 规则版 dataset agent 是否正常路由样本，并且只在适配题型上启用 Stage 5。
- 做完后要检查什么：
  - `sofar/output/agent_debug/spatialbench/<run_id>/` 已生成
  - `sofar/output/eval_spatialbench_progress.json` 中出现：
    - `agent_decision_distribution`
    - `agent_verification_distribution`
    - `agent_selected_execution_mode_distribution`
    - `agent_selected_dataset_agent_distribution`
  - 日志里出现 `agent decision=...`、`agent verify=...`
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_debug`
- 必要补充文件：
  - `sofar/output/eval_spatialbench_progress.json`
  - 本轮最新的 `sofar/output/eval_spatialbench_*.log`

### Step 1B：SpatialBench `stage5_applicable14` 定向补 cache 与 targeted smoke
- 命令：

```bash
cd /data/coding/SoFar
python spatialbench/eval_spatialbench.py --pilot stage5_applicable14 --stage3-grounding-only --speed-profile conservative
python spatialbench/eval_spatialbench.py --pilot stage5_applicable14 --stage4-pointdata-only --speed-profile conservative
python spatialbench/eval_spatialbench.py --pilot stage5_applicable14 --reset-progress --use-stage5-head --agent-mode dataset --agent-save-trace --speed-profile conservative
```

- 意义：这一步专门验证 `SpatialBench` 中真正适合 `Stage 5` 的定向 pilot 子题，不再受“顺序前 20 条一个都不适配”的影响。
- 做完后要检查什么：
  - 日志里至少出现部分 `stage5 apply`，并能看到 `prompt_variant=axis_direction|reference_alignment|camera_alignment`
  - `eval_spatialbench_progress.json` 里的 `agent_used_stage5_count` 不再恒等于 `0`
  - `run_filter.pilot = stage5_applicable14`
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_debug`
- 必要补充文件：
  - `sofar/output/eval_spatialbench_progress.json`
  - 本轮最新的 `sofar/output/eval_spatialbench_*.log`

### Step 2：Open6DOR 20-case dataset agent smoke
- 命令：

```bash
cd /data/coding/SoFar
python open6dor/open6dor_perception.py --limit 20 --reset-progress --rerun-existing --use-stage5-head --agent-mode dataset --agent-shadow-eval --agent-save-trace --speed-profile conservative
```

- 意义：验证 `Open6DOR` 规则版 dataset agent 是否正确区分 `direct_allow / conditional_verify / baseline_only / shadow`。
- 做完后要检查什么：
  - `sofar/output/agent_debug/open6dor/<run_id>/` 已生成
  - `sofar/output/open6dor_perception_summary.json` 中出现 `agent_summary`
  - `sofar/output/stage5_open6dor_pipeline_records.json` 的每条记录里出现：
    - `agent_decision`
    - `agent_verification_status`
    - `agent_selected_execution_mode`
    - `agent_shadow_used`
    - `agent_shadow_accepted`
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_debug`
- 必要补充文件：
  - `sofar/output/open6dor_perception_summary.json`
  - `sofar/output/stage5_open6dor_pipeline_records.json`
  - `sofar/output/stage5_open6dor_pipeline_records.csv`
  - 本轮最新的 `sofar/output/open6dor_perception_*.log`

### Step 3：SpatialBench 20-case auto agent smoke
- 命令：

```bash
cd /data/coding/SoFar
python spatialbench/eval_spatialbench.py --limit 20 --reset-progress --use-stage5-head --agent-mode auto --agent-save-trace --speed-profile conservative
```

- 意义：验证 `AutoModeSelection Agent` 在 SpatialBench 样本上是否能自动路由到 SpatialBench 专用 controller。
- 做完后要检查什么：
  - `agent_controller_distribution` 中出现 auto router
  - `agent_selected_dataset_agent_distribution` 中 SpatialBench 子 agent 不为空
  - `agent_selected_execution_mode_distribution` 有合理分布
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_debug`
- 必要补充文件：
  - `sofar/output/eval_spatialbench_progress.json`
  - 本轮最新的 `sofar/output/eval_spatialbench_*.log`

### Step 4：Open6DOR 20-case auto agent smoke
- 命令：

```bash
cd /data/coding/SoFar
python open6dor/open6dor_perception.py --limit 20 --reset-progress --rerun-existing --use-stage5-head --agent-mode auto --agent-shadow-eval --agent-save-trace --speed-profile conservative
```

- 意义：验证 `AutoModeSelection Agent` 在 Open6DOR 样本上是否能自动路由到 Open6DOR 专用 controller。
- 做完后要检查什么：
  - `agent_controller_distribution` 中出现 auto router
  - `agent_selected_dataset_agent_distribution` 中 Open6DOR 子 agent 不为空
  - `agent_selected_execution_mode_distribution` 能看到 `stage5_direct_verified / stage5_conditional_verified / stage5_shadow_only / baseline_only` 之类的执行带分布
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_debug`
- 必要补充文件：
  - `sofar/output/open6dor_perception_summary.json`
  - `sofar/output/stage5_open6dor_pipeline_records.json`
  - 本轮最新的 `sofar/output/open6dor_perception_*.log`

### Step 5：Stage 8 eval smoke
- 命令：

```bash
cd /data/coding/SoFar
python analysis/stage8_agent_eval.py --dataset spatialbench --eval-mode baseline --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
python analysis/stage8_agent_eval.py --dataset spatialbench --eval-mode direct_stage5 --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
python analysis/stage8_agent_eval.py --dataset spatialbench --eval-mode agent --agent-mode dataset --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
python analysis/stage8_agent_eval.py --dataset open6dor --eval-mode baseline --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
python analysis/stage8_agent_eval.py --dataset open6dor --eval-mode direct_stage5 --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
python analysis/stage8_agent_eval.py --dataset open6dor --eval-mode agent --agent-mode dataset --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
```

- 意义：验证 Stage 8 独立 runner 是否能在不依赖训练脚本的情况下，产出 baseline / direct_stage5 / agent 三组可对比结果。
- 做完后要检查什么：
  - `sofar/output/agent_eval_smoke/spatialbench/baseline/summary.json`
  - `sofar/output/agent_eval_smoke/spatialbench/direct_stage5/summary.json`
  - `sofar/output/agent_eval_smoke/spatialbench/agent/summary.json`
  - `sofar/output/agent_eval_smoke/open6dor/baseline/summary.json`
  - `sofar/output/agent_eval_smoke/open6dor/direct_stage5/summary.json`
  - `sofar/output/agent_eval_smoke/open6dor/agent/summary.json`
  - 每个目录下都有：
    - `summary.json`
    - `per_sample.jsonl`
    - `metrics_by_bucket.json`
    - `decision_breakdown.csv`
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_eval_smoke`

### Step 6：Stage 8 ablation smoke
- 命令：

```bash
cd /data/coding/SoFar
python analysis/stage8_agent_ablation.py --dataset spatialbench --agent-mode dataset --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
python analysis/stage8_agent_ablation.py --dataset open6dor --agent-mode dataset --limit 20 --output-dir /data/coding/SoFar/output/agent_eval_smoke
```

- 意义：一口气把三组模式打包成消融结果，生成可以直接拿来对比的总表。
- 做完后要检查什么：
  - `sofar/output/agent_eval_smoke/ablation_summary.json`
  - `sofar/output/agent_eval_smoke/ablation_table.csv`
  - 对应数据集子目录里的三组 summary 仍然存在
- 做完后要回传哪些文件夹：
  - `sofar/output/agent_eval_smoke`

## 6. 上传/回传文件夹规则

### 6.1 最小代码同步
- 上传优先写文件夹：
  - `sofar/serve`
  - `sofar/spatialbench`
  - `sofar/open6dor`
  - `sofar/analysis`
- 如果服务器训练代码或 checkpoint 不一致，再补传：
  - `sofar/orientation`
  - `sofar/output/stage5_open6dor_train`
  - `sofar/output/stage5_spatialbench_train`
- 这一步通常不要求回传结果文件夹。

### 6.2 agent smoke
- 上传优先写文件夹：
  - `sofar/serve`
  - `sofar/spatialbench`
  - `sofar/open6dor`
  - `sofar/analysis`
- 回传优先写文件夹：
  - `sofar/output/agent_debug`
- 必要补充文件：
  - `sofar/output/eval_spatialbench_progress.json`
  - 本轮最新 `sofar/output/eval_spatialbench_*.log`
  - `sofar/output/open6dor_perception_summary.json`
  - `sofar/output/stage5_open6dor_pipeline_records.json`
  - `sofar/output/stage5_open6dor_pipeline_records.csv`
  - 本轮最新 `sofar/output/open6dor_perception_*.log`

### 6.3 Stage 8 eval / ablation
- 上传优先写文件夹：
  - `sofar/serve`
  - `sofar/spatialbench`
  - `sofar/open6dor`
  - `sofar/analysis`
- 回传优先写文件夹：
  - `sofar/output/agent_eval_smoke`
- 必要补充文件：
  - 如果 Stage 8 runner 中途失败，再补充本轮最新 `sofar/output/eval_spatialbench_*.log` 与 `sofar/output/open6dor_perception_*.log`

## 7. 当前缺口与下一步

### 本地已完成但尚未做服务器验证的
- `semantic_orientation_agent.py` 的 dataset / auto 决策分流
- `open6dor_perception.py` 的 conditional verify / shadow path
- `agent_debug.py` 的集中 trace 写盘
- `stage8_agent_eval.py` 与 `stage8_agent_ablation.py` 的独立评测框架

### 已经留好接口但暂时不默认执行的
- `ROI rebuild / ROI rescale`
- `part-first grounding` 的扩展动作
- SpatialBench 的 second-pass 推理
- Open6DOR 更激进的 extended actions

### 当前不再优先推进的
- 新的 mixed training 变体
- 更大规模 shared head 训练
- 在没有先验证 agent 的情况下继续扩大 100-case / full-set 直接注入实验

### 服务器 smoke 完成后的自然下一步
- 如果 `dataset agent` 与 `auto agent` 都稳定，就用 Stage 8 输出的三组表先比较：
  - `decision distribution`
  - `verification distribution`
  - `selected_execution_mode distribution`
  - `stage5_usage_precision`
  - `fallback_precision`
  - `shadow acceptance`
  - `bucket / mode` 分桶结果
- 只有当这一步确认 agent 行为和预期一致后，再决定是否扩到更大样本规模。
