当前结论：
1. `Stage 2` 已完成并关闭。
2. `Stage 3` 已完成并关闭。
3. `Stage 4` 已完成工程收口，并已把 smoke cache 交接给 `Stage 5`。
4. 当前不要再重复跑 `Stage 2/3/4` 的 `10-case / 50-case`。
5. 下一步主线已经切换到 `Stage 5`。

本次本地新增内容：
1. `Stage 4 -> Stage 5 manifest` 工具
   - `sofar/serve/stage5_manifest.py`
2. `Stage 5 Dataset/DataLoader`
   - `sofar/orientation/datasets/Stage4PointCache.py`
3. `Part-conditioned Orientation Head` 最小 dry-run 模型
   - `sofar/orientation/models/PartConditionedOrientationHead.py`
4. `Stage 5` 最小 dry-run 入口
   - `sofar/orientation/stage5_dry_run.py`
5. 相关注册文件
   - `sofar/orientation/datasets/__init__.py`
   - `sofar/orientation/models/__init__.py`

本地已经完成的验证：
1. 已生成：
   - `sofar/output/stage5_manifest_spatialbench_smoke.jsonl`
   - `sofar/output/stage5_manifest_open6dor_smoke.jsonl`
   - `sofar/output/stage5_manifest_summary.json`
2. manifest 当前状态：
   - `SpatialBench`：`10/10` 本地可用
   - `Open6DOR`：`0/10` 本地可用
   - 原因：本地没有同步逐任务 `output/stage4` cache，只同步了 records 文件
3. 已完成一次本地最小 dry-run：
   - 结果文件：`sofar/output/stage5_dry_run_summary.json`
   - 结果摘要：
     - `dataset_size = 4`
     - `batch_size = 2`
     - `num_points = 1024`
     - `device = cpu`
     - `loss = 0.470613`
   - 说明 `manifest -> Dataset/DataLoader -> model -> loss/backward` 全链可跑

注意：
1. 当前 `Stage 5` dry-run 使用的是最小 smoke 版 `PartConditionedOrientationHead`
2. 现在能证明的是训练链入口成立，不是最终 benchmark 性能已经提升
3. 当前最值得做的是云服务器上的 GPU dry-run，而不是回头重复 Stage 2/3/4

下次需要上传到云服务器的文件：
1. `sofar/serve/stage5_manifest.py`
2. `sofar/orientation/datasets/Stage4PointCache.py`
3. `sofar/orientation/datasets/__init__.py`
4. `sofar/orientation/models/PartConditionedOrientationHead.py`
5. `sofar/orientation/models/__init__.py`
6. `sofar/orientation/stage5_dry_run.py`

下次到云服务器后的执行顺序：
1. 进入项目目录
`cd /data/coding/SoFar`

2. 不需要再跑 Stage 2/3/4

3. 直接执行 Stage 5 manifest + dry-run
`python orientation/stage5_dry_run.py --repo-root /data/coding/SoFar --batch-size 2 --num-points 1024 --max-samples 8 --device cuda`

这条命令会自动做：
1. 从现有 `stage4_*_point_records.json` 生成 smoke manifest
2. 优先选择当前可用的 manifest
3. 构建 `Stage 5 Dataset/DataLoader`
4. 跑一个最小 batch 的 forward/backward
5. 输出：
   - `output/stage5_manifest_spatialbench_smoke.jsonl`
   - `output/stage5_manifest_open6dor_smoke.jsonl`
   - `output/stage5_manifest_summary.json`
   - `output/stage5_dry_run_summary.json`

下次执行后需要同步回本地的内容：
1. `sofar/output/stage5_manifest_summary.json`
2. `sofar/output/stage5_dry_run_summary.json`
3. 如果云服务器上已经能访问 Open6DOR 的逐任务 `stage4` cache，也一并同步：
   - `sofar/output/stage5_manifest_open6dor_smoke.jsonl`

下次我接手时会优先看：
1. 云服务器 Stage 5 dry-run 是否 GPU 跑通
2. Open6DOR manifest 在服务器上是否从 `0` 变成可用
3. loss、batch、样本选择是否稳定
4. 然后再决定是否进入更正式的小规模训练
