当前默认就是本地 Qwen，服务器命令不需要再执行 `unset SOFAR_LLM_BACKEND`。

本次最新修正：
1. `Stage 2 Open6DOR` 的 50-case 独立 parser 结果已经同步回本地，运行层面 `50/50` 跑通。
2. 但语义检查发现约 `26/50` 条样本存在 `picked_object` 与参考物体反转的问题，例如：
   - 指令：`Place the USB behind the bottle...`
   - 旧结果：`picked_object = bottle`，`related_objects = ["USB"]`
3. 本地已完成两处修复：
   - `sofar/serve/qwen_inference.py`：当 `task_config.target_obj_name` 存在时，强制将其作为 `picked_object`，并把模型误放进去的参考物体并回 `related_objects`。
   - `sofar/serve/system_prompts.py`：补强 prompt，明确要求 `picked_object` 必须与 `task_config.target_obj_name` 一致。
4. 因此：
   - `Stage 2` 现在**不能直接标记为结束**，但已经只差一次很短的校正重跑。
   - 之前跑出来的 `Open6DOR Stage 3/4` 结果只能算临时 smoke，不作为正式基线。
   - `SpatialBench Stage 3` 的 OOM 结论也不能写死，因为那次是两个 test 同时跑导致的并发占卡。

如果服务器已经有上一轮 Stage 2/3/4 代码，本轮至少重新上传：
- `sofar/serve/qwen_inference.py`
- `sofar/serve/system_prompts.py`

如果你想保险一点，把这批一起重传也可以：
- `sofar/serve/system_prompts.py`
- `sofar/serve/qwen_inference.py`
- `sofar/serve/stage3_grounding.py`
- `sofar/serve/stage4_point_data.py`
- `sofar/spatialbench/eval_spatialbench.py`
- `sofar/open6dor/open6dor_perception.py`

建议上传到服务器对应目录：
- `/data/coding/SoFar/serve`
- `/data/coding/SoFar/spatialbench`
- `/data/coding/SoFar/open6dor`

上传后在服务器手动执行：

注意：下面命令是直接输入终端的，不要把命令前后两侧的反引号 `` ` `` 一起复制进去；否则 bash 会把输出当成命令替换，出现 `bash: [batch-log]: command not found` 之类的假报错。

1. 进入项目目录
`cd /data/coding/SoFar`

2. 先补 Stage 2 的关键校正重跑（这是下一次最优先的命令）
`python open6dor/open6dor_perception.py --stage2-parser-only --limit 50 --speed-profile conservative`

3. 重跑后重点检查：
- `output/stage2_open6dor_parser_records.json`
- `output/stage2_open6dor_parser_records.csv`
- `picked_object` 是否与 `task_config.target_obj_name` 对齐
- `related_objects` 是否保留真正参考物体，而不是把目标物体塞进去

4. 如果 Stage 2 校正重跑通过，再继续 Stage 3
- Open6DOR 先跑 10 条：
`python open6dor/open6dor_perception.py --stage3-grounding-only --limit 10 --speed-profile conservative`
- 稳定后扩到 50 条：
`python open6dor/open6dor_perception.py --stage3-grounding-only --limit 50 --speed-profile conservative`

5. 然后再跑 Stage 4
- Open6DOR 先跑 10 条：
`python open6dor/open6dor_perception.py --stage4-pointdata-only --limit 10 --speed-profile conservative`
- 稳定后扩到 50 条：
`python open6dor/open6dor_perception.py --stage4-pointdata-only --limit 50 --speed-profile conservative`

6. SpatialBench 的 Stage 3/4 下次也要补一次，但前提是不要和别的 test 并发占卡。
- SpatialBench Stage 3：
`python spatialbench/eval_spatialbench.py --stage3-grounding-only --limit 10 --speed-profile conservative`
`python spatialbench/eval_spatialbench.py --stage3-grounding-only --limit 50 --speed-profile conservative`
- SpatialBench Stage 4（依赖新的 Stage 3 cache）：
`python spatialbench/eval_spatialbench.py --stage4-pointdata-only --limit 10 --speed-profile conservative`
`python spatialbench/eval_spatialbench.py --stage4-pointdata-only --limit 50 --speed-profile conservative`

本轮维护结论：
- `Stage 2`：运行已通，但 Open6DOR 语义对齐需一次短重跑确认，暂不正式完结。
- `Stage 3`：骨架没问题，Open6DOR 结果需在 Stage 2 修复后重跑；SpatialBench 需排除并发干扰后复核。
- `Stage 4`：逻辑已通，但有效性依赖新的 Stage 3 cache，当前旧 Open6DOR 结果只算 smoke。
