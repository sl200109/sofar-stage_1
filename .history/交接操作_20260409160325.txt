当前默认就是本地 Qwen，后续服务器命令不需要再执行 unset SOFAR_LLM_BACKEND。

Stage 1 当前进度：
spatialbench全量跑通、open6dor实操数据集跑了一百个，目前进行效率优化，单次open6dor耗时约一分钟一个。

上传后，使用保守策略，现在已经改成了调用一次qwen，并且加入了缓存机制。
  把 Open6DOR 主批处理默认改成了：
  最小对象集合 -> detection/SAM/scene graph -> 单次 joint Qwen
  保留旧双阶段路径作为 fallback，joint 失败会自动退回
  接入单案例感知缓存：
  output/perception_cache.json
  output/mask_cache.npz
  接入 orientation 半模板化：
  orientation_templates.json
  接入 lightweight scene graph 给最终推理用
  接入最小对象集合裁剪：
  默认只保留 picked object + 1~2 related objects
  between / center / 模板命中差 这类复杂情况自动回退旧路径


下次上传服务器后做：
cd /data/coding/SoFar

spatialbench运行
python spatialbench/eval_spatialbench.py --reset-progress --speed-profile conservative

open6dor运行
python open6dor/open6dor_perception.py --pilot open6dor10 --reset-progress --speed-profile conservative

最后汇总
python scripts/stage1_collect_baseline.py --hard-case-limit 100
