当前结论：
1. `Stage 2` 已完成并关闭。
2. `Stage 3` 已完成并关闭。
3. `Stage 4` 已完成并关闭。
4. `Stage 5 dry-run` 已完成并关闭。
5. 当前真正的瓶颈不是训练入口，而是**训练数据量不足**。
6. 所以下一轮**不要直接拿 10-case cache 开始训练**，而是先扩充 `Stage 4` cache。

以后上传统一按文件夹：
1. 上传整个：
`sofar/serve`
2. 上传整个：
`sofar/orientation`
3. 上传整个：
`sofar/open6dor`

当前已经完成但暂不急着执行的入口：
1. `orientation/stage5_train_pilot.py`
2. 这个入口会保留，等数据量扩大后再用

下一步优先级：
## 第一优先级：补训练数据


### 1. SpatialBench 先扩到 100-case
继续执行：
`python spatialbench/eval_spatialbench.py --stage3-grounding-only --limit 100 --speed-profile conservative`
`python spatialbench/eval_spatialbench.py --stage4-pointdata-only --limit 100 --speed-profile conservative`


### 2. Open6DOR 先扩到 100-case
先执行：
`cd /data/coding/SoFar`

`python open6dor/open6dor_perception.py --stage3-grounding-only --limit 100 --speed-profile conservative`
`python open6dor/open6dor_perception.py --stage4-pointdata-only --limit 100 --speed-profile conservative`


说明：
1. `Open6DOR` 这两条命令现在默认是**增量补跑**
2. 它会先跳过已经有有效 `Stage 3/Stage 4 cache` 的任务，再对剩余缺失任务应用 `--limit 100`
3. 所以这一步的目标不是再检查 Stage 3/4 正不正常，而是**扩大训练 cache 覆盖范围**
4. `SpatialBench` 仍然按覆盖式方式跑，我方接受它不增量
5. 当前先到 `100-case`，不要一口气冲全量
6. 等这批结果稳定后，再决定要不要把 SpatialBench 扩到全量 `223`

如确实需要强制重跑 `Open6DOR Stage 3/4` 已有 cache，再手动设置：
`export SOFAR_OPEN6DOR_FORCE_STAGE_RERUN=1`
默认不要开这个环境变量。

## 第二优先级：同步回本地
执行完上面的 4 条命令后，需要同步回本地的重点内容：
1. `sofar/output/stage4_open6dor_point_records*.json`
2. `sofar/output/stage4_spatialbench_point_records*.json`
3. 如方便，也同步新的 `output/stage4_spatialbench_point_cache/`
4. 如方便，也同步 Open6DOR 逐任务 `output/stage4/` cache 所在目录摘要或清单

## 第三优先级：再启动 Stage 5 tiny-train pilot
只有在 `Stage 4` cache 扩量完成后，才执行：

`python orientation/stage5_train_pilot.py --repo-root /data/coding/SoFar --dataset-mode combined --batch-size 2 --num-points 1024 --max-train-samples 32 --max-val-samples 8 --epochs 12 --device cuda`

当前不要提前执行这条命令。

当前本地已经完成的工作：
1. `Stage 5` 训练标签定义已经加入 manifest：
   - primary：`geometry_priors.part_to_object_vector`
   - fallback：`orientation_mode template axis`
   - 并加入 `train_label_confidence`
2. 已有正式 tiny-train 入口：
   - `sofar/orientation/stage5_train_pilot.py`
3. 但该入口当前是“等数据扩量后启用”，不是现在立刻跑

如果下次继续由我接手，我会直接做三件事：
1. 看扩量后的 `Stage 4` records 是否足够支撑训练
2. 评估 Open6DOR / SpatialBench 的可用训练样本规模
3. 然后正式启动第一轮 `Stage 5 tiny-train pilot`
